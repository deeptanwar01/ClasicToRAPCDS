projection;
strict ( 2 );
use draft;

define behavior for ZDAY3_C_STUDENT_UM alias Students
{
  use create;
  use update;
  //use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
  use action setStatus;
}